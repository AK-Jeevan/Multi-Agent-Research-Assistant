services:
  research-assistant:
    build: .
    env_file:
      - path: .env
        required: false
    ports:
      - "8000:8000"
    volumes:
      - ./data/chroma:/app/data/chroma
      - ./data/trajectories:/app/data/trajectories
    restart: unless-stopped
